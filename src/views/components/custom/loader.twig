{# {% if theme.settings.get('enable_loader_screen') %}
<div class="loader-init" id="page-loader">
	{% if theme.settings.get('loader_type') == 'logo_loader' %}
	<div class="loader-container">
		<div class="glow"></div>
		<div class="text">
		{% if theme.settings.get('change_loder') %}
		{% if theme.settings.get('custom_loader_image') %}
			<img src="{{ theme.settings.get('custom_loader_image') }}" alt="{{ store.name }}" class="h-12 mx-auto" />
			{% endif  %}
		{% else %}
		{% if store.logo %}
			<img src="{{ store.logo }}" alt="{{ store.name }}" class="h-12 mx-auto" />
			{% endif %}
		{% endif %} 
		</div>
		<div class="orbit-container">
			<div class="semicircle"></div>
			<div class="ball"></div>
		</div>
	</div>
	{% elseif theme.settings.get('loader_type') == 'custom_loader' %}
	<div class="smart-loader-container">
		<div class="data-dot"></div>

		<svg class="circuit-leaf" viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg">
			<path class="leaf-path" d="M50 95 C 10 70, 0 40, 50 5 C 100 40, 90 70, 50 95 Z" />
			<path class="vein-path" d="M50 95 L 50 20" />
			<path class="vein-path" d="M50 70 L 25 55" />
			<path class="vein-path" d="M50 60 L 75 45" />
			<path class="vein-path" d="M50 45 L 30 35" />
			<circle class="node-point" cx="25" cy="55" r="2" />
			<circle class="node-point" cx="75" cy="45" r="2" />
			<circle class="node-point" cx="30" cy="35" r="2" />
			<circle class="node-point" cx="50" cy="20" r="2" />
		</svg>

		<div class="smart-text">{{store.name}}...</div>
	</div>
	{% endif %}
</div>
{% endif %} #}